mutation savePaypalSubscription(
    $paypalPlanId: String!,
    $paypalSubscriptionId: String!
) {
    savePaypalSubscription(paypalPlanId: $paypalPlanId, paypalSubscriptionId: $paypalSubscriptionId) {
        id
    }
}
